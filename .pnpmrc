# pnpm configuration for container/host compatibility
# Ensures esbuild and other native binaries work in both environments

# Install all supported architectures for native dependencies
# This fixes issues where container installs Linux binaries but host needs macOS binaries
supported-architectures=os=linux,darwin,win32&cpu=arm64,x64

# Enable automatic peer dependency installation
auto-install-peers=true

# Use faster symlink strategy for better performance
node-linker=hoisted